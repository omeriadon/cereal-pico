{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & Flash",
            "type": "shell",
            "command": "cmake --build build && sudo cp build/swift-blinky.uf2 /Volumes/RPI-RP2",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
